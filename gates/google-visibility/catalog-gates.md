# Gates: Complete Dude catalog pagination
- [x] C1: Products, variants and images paginate beyond existing50/50/10 limits without omissions/duplicates and preserve normalized behavior.
  EVIDENCE: `lib/shopify-catalog.js` uses Shopify forward cursor pagination for all three connections, appends in API order, rejects invalid/duplicate nodes and stalled/incomplete cursors, and normalizes only after every page succeeds. Nullable Shopify image IDs use a namespaced URL fallback. Shopify references: https://shopify.dev/docs/api/usage/pagination-graphql and https://shopify.dev/docs/api/storefront/latest/objects/image
- [x] C2: Small runnable regression covers independent pagination boundaries and failed upstream pages; existing relevant tests pass.
  EVIDENCE: `node scripts/test-shopify-catalog-pagination.js` passes with 52 products, 52 variants, 12 images including null IDs across pages, repeated URL rejection, missing/repeated cursor rejection, product/variant/image page failures, a no-partial-cache retry, exact complete stale fallback after 46 seconds, and rejection beyond five minutes. Existing `test-merch-product-order.js`, `test-google-product-feed.js`, `test-product-route.js`, `test-merch-discovery.js`, `test-merch-product-details.js`, and `test-merch-measurement.js` all pass.
- [x] C3: Parent independent review and rerun pass; production acceptance remains separate.
  EVIDENCE: Parent inspected nullable-ID correction and reran pagination including malformed cursor/stale tests; PASS. Independent review schema_research identified the corrected defect; deployment remains separate.
