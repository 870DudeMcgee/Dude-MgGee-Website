# Gates: Dude shared fulfillment and schema
- [x] D1: Feed and page schema share verified profile facts for every public product including Checkout Girl; no silent unclassified product omission.
  EVIDENCE: `lib/product-policies.js` is the single source for the 11-product public baseline. Feed and Offer schema parity is exercised for every mapped handle. An unknown handle throws `Missing verified product policy` and makes the feed endpoint return 503; the page-schema regression proves an unmapped product remains sellable without publishing an invented policy. Checkout Girl uses verified Shopify shipping profile `133064753459`, Shopify product `10448707715379`, Printful store `18505090`, synced product `470134813`, product code `PF-FRG1001`, all nine XS–5XL variants, `Heather gray`, `unisex`, and $4.95 US shipping. Option values override shared fallbacks.
- [x] D2: Self-contained valid policy/shipping markup preserves no-general-returns policy, truthful timings, all variants and existing product shape; no fake reviews/identifiers.
  EVIDENCE: Every mapped Offer, including sold-out offers, now carries `OfferShippingDetails` with US price, 2–5-day handling, 1–8-day transit, and Monday–Friday `businessDays`, plus an offer-level US `MerchantReturnPolicy` using `MerchantReturnNotPermitted` and `https://www.dudemcgee.com/returns.html`. Tests assert no `merchantReturnDays`, reviews, or ratings were introduced and existing SKUs remain intact. This follows Google's [merchant listing shipping/return properties](https://developers.google.com/search/docs/appearance/structured-data/merchant-listing) and Schema.org's [ShippingDeliveryTime businessDays contract](https://schema.org/ShippingDeliveryTime); external Rich Results processing remains part of D4.
- [x] D3: Image sitemap includes full and variant-only images; feed additional images use only real catalog images within Google limits.
  EVIDENCE: Feed items deduplicate real product and variant image URLs, exclude the selected primary image, and cap `additional_image_link` at 10 as specified by Google's [additional image link documentation](https://support.google.com/merchants/answer/6324370). The sitemap now includes and deduplicates variant-only images. Boundary regressions cover 12 catalog images, a variant primary, and a variant-only sitemap image.
- [x] D4: Relevant regressions and independent parent review pass; account approval separate.
  EVIDENCE: Independent schema_research review PASS with no blocking findings; parent inspected the diff and reran pagination, feed, route/browser behavior, sitemap and SEO checks, all PASS. The changed code against live Shopify produced 11 products, 64 offers, 64 feed items and 64 shipping blocks. Deployment, Rich Results interpretation and Google processing remain separate recovery gates, not asserted by this code-review gate.
